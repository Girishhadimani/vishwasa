# Application Configuration
spring.application.name=vishwasa
server.port=${PORT:8080}

# Database Configuration (Supabase Managed Cloud PostgreSQL Database)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.ebwwcboxyuymkdhgcehc.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:bY=@BbV+=/x9ing}
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Pool Configuration (Robust Cloud Retries)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=0

# JPA & Explicit Hibernate Dialect Configuration (Prevents Dialect Lookup Crash on Startup)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (Supports Cloud Env Vars with Strong 256-Bit Secret Key Default)
jwt.secret=${JWT_SECRET_KEY:${JWT_SECRET:VishwasaHealthcareFoundationSuperSecretKey2026NorthKarnatakaBelagaviHQ98450SecureJWTTokenSigningKey32BytesLong}}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Outbound Email Service (Resend API)
resend.api.key=${RESEND_API_KEY:YOUR_RESEND_API_KEY}

# Supabase Platform Keys
supabase.url=https://ebwwcboxyuymkdhgcehc.supabase.co
supabase.publishable.key=sb_publishable_0YGPYMwmlC_B574yXOSv3Q_AcPY-EZi
supabase.jwks.url=https://ebwwcboxyuymkdhgcehc.supabase.co/auth/v1/.well-known/jwks.json

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.vishwasa=DEBUG
logging.level.org.springframework.security=DEBUG
